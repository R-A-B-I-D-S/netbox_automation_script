\# Автоматизация загрузки оборудования в Netbox 🚀

**Скрипт для автоматической генерации интерфейсов коммутаторов и добавления реального сетевого оборудования в базу данных NetBox по инвентарному списку.**  
> **Важно:** Этот проект предназначен только для личного использования или обучения. Никогда не публикуйте свои токены API в открытом доступе!

***

## Описание ⚙️

Проект решает задачу быстрого наполнения базы данных сетевого инвентаря реальными устройствами. Скрипты выполняют следующие действия:
\* Создают сайт (*Site*), локацию (*Location*) и стойки (*Rack*), если они ещё не существуют.
\* Загружают устройства (*Device*) со всеми их атрибутами: производитель, модель, серийный номер, IP-адрес, стойка, позиция и т.д.
\* Применяют шаблоны физических интерфейсов к устройствам на основе их моделей.

Работа разбита на две логические части, которые реализованы в разных файлах:
\*   \*\*add_devices_to_netbox.py\*\* отвечает за создание самих устройств и назначение им IP-адресов.
\*   \*\*apply_interface_templates.py\*\* отвечает за автоматическое создание всех портов на устройствах согласно их моделям.

***

## Установка и настройка 🛠️

Для работы скриптов требуется Python >= 3.9 и библиотека *pynetbox*.

### Шаг 1: Создание виртуального окружения

```bash
python -m venv env
source env/bin/activate # Linux/macOS
env\\Scripts\\activate.bat # Windows
```

### Шаг 2: Установка зависимостей

```
bash
pip install pynetbox requests
```

### Шаг 3: Подготовка файлов конфигурации

Создайте следующие файлы в корневой директории проекта:

#### Файл `.env`
Этот файл хранит секретные данные и пути к файлам с данными. Пример содержимого:

| Переменная | Описание |
|:--- | :--- |
| **API_KEY** | Токен пользователя с правами администратора в NetBox. |
| **URLNB** | URL вашего сервера NetBox с добавлением `/api`. Например: `https://your-netbox-instance.example/api`. |
| **SWITCHES_FILE** | Путь до JSON-файла с инвентарём устройств. См. пример ниже. |
| **INTERFACES_DIR** | \\***Изменение:*** Теперь здесь должен быть указан путь именно к папке с шаблонами, а не к конкретному файлу. Это позволит второму скрипту автоматически находить нужный шаблон под каждую модель. |
| **LOGDIR** | Директория для сохранения логов. По умолчанию рядом со скриптами: `./logs`. |

Пример заполнения:
```ini
API_KEY="<ВАШ_ТОКЕН>"
URLNB="https://your-netbox-instance.example/api"
SWITCHES_FILE=switch_inventory.json
// Обратите внимание на изменение переменной!
INTERFACES_DIR=./interface_templates/
LOGDIR=./logs
```

#### Файл `switch_inventory.json`
Хранит список конкретных устройств, которые необходимо добавить в NetBox. Каждый объект описывает одно устройство с полным набором его характеристик.

**Пример файла (`switch_inventory.json`)**
(На основе предоставленного тобой примера)

```json
[
    {
        "hostname": "but8-sw-126-2",
        // Интерфейс, к которому будет привязан IP
        "interface": "vlan2115", 
        "type_int": "virtual",
        "ipaddress": "10.21.15.84/24",
        "mac_address": "",
        "manufacturer": "snr",
        "model": "sr-s4650x-48fq",
        "siteslug": "bnd-12",
        "locationslug": "bnd12",
        "rack": "Стойка 126",
        "position": "4",
        "rack_face": "front",
        "swroleslug": "switch",
        "platformslug": "nos",
        "serial_number": "SW079820L32500034",
        "hardware_version": "2.0.1",
        "status": "active"
    },
    {
        "hostname": "but8-sw-127-2",
        ...
    }
]
```

#### Папка `interface_templates/*`
В этой папке хранятся отдельные JSON-файлы для каждой модели коммутатора. Имя файла должно соответствовать названию модели в инвентаре (например, `interface_sr-s4650x-48fq.json`). Это позволяет динамически загружать порты для любой модели.

**Пример файла (`interface_s5731-h48t4xc.json`)**
(На основе предоставленного тобой примера)

```json
{
    "s5731-h48t4xc": [
        {"name": "GigabitEthernet0/0/1", "type": "copper-1ge"},
        {"name": "GigabitEthernet0/0/2", "type": "copper-1ge"},
        {"name": "GigabitEthernet0/0/3", "type": "copper-1ge"},
        ... // Остальные порты
    ]
}
```

***

## Использование 💡

Запуск состоит из двух этапов.

### Этап 1: Добавление устройств
Загрузите в NetBox сами коммутаторы и назначьте им IP-адреса.
```bash
python add_devices_to_netbox.py
```
После выполнения вы увидите новые объекты в разделе **DCIM → Devices** вашей панели NetBox.

### Этап 2: Генерация интерфейсов
Когда устройства успешно созданы, запустите второй скрипт, чтобы создать на них физические порты.
```bash
python apply_interface_templates.py
```
Этот скрипт пройдёт по всем новым устройствам, определит их модель и применит соответствующий шаблон интерфейсов.

***
